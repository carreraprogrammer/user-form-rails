<h1>Users</h1>

<%= link_to "Crear PDF de usuarios", users_path(format: :pdf), class: "pdf-btn"%>

<table class="user-info-table">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Apellido</th>
      <th>Nacimiento</th>
      <th>Email</th>
      <th>Dirección</th>
      <th>Casa/Apartamento</th>
      <th>Sexo</th>
      <th>Ciudad</th>
      <th>Pais</th>
      <th>Departamento</th>
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <%= render user %>
    <% end %>
  </tbody>
</table>

<%= link_to "New user", new_user_path, class: "btn btn-primary new-user" %>
